diff --git a/node_modules/@jupyterlite/cockle/lib/tools/prepare_wasm.js b/node_modules/@jupyterlite/cockle/lib/tools/prepare_wasm.js
index 65e170e..8b82d4f 100644
--- a/node_modules/@jupyterlite/cockle/lib/tools/prepare_wasm.js
+++ b/node_modules/@jupyterlite/cockle/lib/tools/prepare_wasm.js
@@ -96,59 +96,65 @@ const wasmPackageNames = Object.entries(cockleConfig.packages)
     .filter(([key, item]) => isWasmPackage(item) && !isLocalPackage(item))
     .map((item) => item[0]);
 console.log('Required WebAssembly package names', wasmPackageNames);
-// Find micromamba.
-let micromambaCmd;
-let micromambaVersion = '';
-const cmds = ['micromamba', '$CONDA_PREFIX/bin/micromamba', '$MAMBA_EXE'];
-for (const cmd of cmds) {
-    try {
-        micromambaVersion = execSync(`${cmd} --version`).toString();
-        micromambaCmd = cmd;
-        break;
-    }
-    catch (e) {
-        // Try next cmd
-    }
-}
-if (micromambaCmd === undefined) {
-    throw new Error('Unable to find micromamba, aborting');
-}
-else {
-    console.log(`Found micromamba: ${micromambaCmd} ${micromambaVersion}`);
-}
-// Create or reuse existing mamba environment for the wasm packages.
+// Find micromamba and fetch wasm packages, but only if any are actually required -- an
+// all-local_directory config (as used by gen's wasm-demo/) needs none of this.
 const envPath = `./${ENV_NAME}`;
-let wasmPackageInfo;
-if (fs.existsSync(envPath)) {
-    wasmPackageInfo = getWasmPackageInfo(micromambaCmd, envPath);
-    const envPackageNames = wasmPackageInfo.map((x) => x.name);
-    const haveAllPackages = wasmPackageNames.every((name) => envPackageNames.includes(name));
-    if (haveAllPackages) {
-        console.log(`Using existing environment in ${envPath}`);
+let wasmPackageInfo = [];
+if (wasmPackageNames.length > 0) {
+    let micromambaCmd;
+    let micromambaVersion = '';
+    const cmds = ['micromamba', '$CONDA_PREFIX/bin/micromamba', '$MAMBA_EXE'];
+    for (const cmd of cmds) {
+        try {
+            micromambaVersion = execSync(`${cmd} --version`).toString();
+            micromambaCmd = cmd;
+            break;
+        }
+        catch (e) {
+            // Try next cmd
+        }
+    }
+    if (micromambaCmd === undefined) {
+        throw new Error('Unable to find micromamba, aborting');
     }
     else {
-        console.log(`Deleting environment in ${envPath} as it does not contain all the required packages`);
-        rimrafSync(envPath);
-        wasmPackageInfo = undefined;
+        console.log(`Found micromamba: ${micromambaCmd} ${micromambaVersion}`);
     }
-}
-if (wasmPackageInfo === undefined) {
-    // Add version restrictions to wasmPackageNames.
-    const packageNamesAndVersions = wasmPackageNames.map(name => {
-        const version = cockleConfig.packages[name]['version']?.trim();
-        if (version) {
-            const modifier = version.match(/^\d+/) ? '=' : '';
-            return `"${name}${modifier}${version}"`; // In quotes to avoid problems with whitespace.
+    // Create or reuse existing mamba environment for the wasm packages.
+    if (fs.existsSync(envPath)) {
+        wasmPackageInfo = getWasmPackageInfo(micromambaCmd, envPath);
+        const envPackageNames = wasmPackageInfo.map((x) => x.name);
+        const haveAllPackages = wasmPackageNames.every((name) => envPackageNames.includes(name));
+        if (haveAllPackages) {
+            console.log(`Using existing environment in ${envPath}`);
         }
-        return name;
-    });
-    const suffix = `--platform=${PLATFORM} ${getChannelsString()}`;
-    console.log(`Creating new environment in ${envPath}`);
-    const createEnvCmd = `${micromambaCmd} create -p ${envPath} -y ${packageNamesAndVersions.join(' ')} ${suffix}`;
-    console.log(createEnvCmd);
-    console.log(execSync(createEnvCmd).toString());
-    // Obtain wasm package info such as version and build string.
-    wasmPackageInfo = getWasmPackageInfo(micromambaCmd, envPath);
+        else {
+            console.log(`Deleting environment in ${envPath} as it does not contain all the required packages`);
+            rimrafSync(envPath);
+            wasmPackageInfo = undefined;
+        }
+    }
+    if (wasmPackageInfo === undefined) {
+        // Add version restrictions to wasmPackageNames.
+        const packageNamesAndVersions = wasmPackageNames.map(name => {
+            const version = cockleConfig.packages[name]['version']?.trim();
+            if (version) {
+                const modifier = version.match(/^\d+/) ? '=' : '';
+                return `"${name}${modifier}${version}"`; // In quotes to avoid problems with whitespace.
+            }
+            return name;
+        });
+        const suffix = `--platform=${PLATFORM} ${getChannelsString()}`;
+        console.log(`Creating new environment in ${envPath}`);
+        const createEnvCmd = `${micromambaCmd} create -p ${envPath} -y ${packageNamesAndVersions.join(' ')} ${suffix}`;
+        console.log(createEnvCmd);
+        console.log(execSync(createEnvCmd).toString());
+        // Obtain wasm package info such as version and build string.
+        wasmPackageInfo = getWasmPackageInfo(micromambaCmd, envPath);
+    }
+}
+else {
+    console.log('All packages are local_directory; skipping micromamba entirely.');
 }
 const outputProps = ['build_string', 'platform', 'version', 'channel'];
 // Insert package info into cockle config.
