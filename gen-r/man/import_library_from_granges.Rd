% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/high_level.R
\name{import_library_from_granges}
\alias{import_library_from_granges}
\title{Import a combinatorial library from GRanges part definitions}
\usage{
import_library_from_granges(
  repo,
  library_name,
  parts_list,
  seq_containers = list(),
  sample = NULL,
  collection = NULL
)
}
\arguments{
\item{repo}{A \code{Repository}.}

\item{library_name}{Character. Library name.}

\item{parts_list}{List of part columns (named character vector, DNAStringSet, or GRanges).}

\item{seq_containers}{List of sequence containers (BSgenome, FaFile, TwoBitFile, or
DNAStringSet with \code{metadata(x)$genome} set) used to resolve GRanges columns.}

\item{sample}{Character or \code{NULL}.}

\item{collection}{Character or \code{NULL}.}
}
\value{
The imported \code{SequenceGraph}.
}
\description{
Like \code{import_library()}, but each column in \code{parts_list} may be a
\code{GRanges} object.  Sequences are extracted via \code{Biostrings::getSeq()}
using the matching \code{seq_containers} entry (matched by genome assembly name
in the \code{Seqinfo} slot).  Named character vector and \code{DNAStringSet}
columns pass through unchanged.
}
\details{
If a \code{GRanges} column has no \code{names()}, part names are
auto-generated from coordinates as \code{"<seqname>:<start>-<end>:<strand>"}.
}
