[package]
name = "gen-models"
description = "Graph-backed models for the gen sequence graph and version control system."
version = "0.2.1"
edition = "2024"
repository = "https://github.com/genhub-bio/gen"
homepage = "https://genhub.bio"
license = "Apache-2.0"

[dependencies]
gen-core = { path = "../gen-core", version = "0.2.1" }
gen-graph = { path = "../gen-graph", version = "0.2.1" }
gen-models-doltlite = { path = "../gen-models-doltlite", version = "0.2.1" }
intervaltree = "0.2.7"
itertools = "0.14.0"
petgraph = "0.6.5"
